{
  "name": "multicalci-material-balance",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "multicalci.com Material Balance Calculator — static UI + Vercel serverless API. Self-contained thermo in api/_lib/*.js. No build step, no runtime dependencies.",
  "scripts": {
    "test": "node tests/run-tests.js"
  }
}
